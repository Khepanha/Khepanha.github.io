function narcissistic(a) {
var powe=a.toString().length;
var ori=a;
var ans=0;
while(ori!=0){
  var re=ori%10;
  ans+=Math.pow(re,powe);
  ori=Math.floor(ori/10);
}
if(ans==a) return true;
else return false;
}
