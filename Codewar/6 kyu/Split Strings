function solution(str){
var str1=[];
    for (var i=0;i<str.length;i+=2)
    {
        str1.push(str.slice(i,i+2));
    }
    if (str.length%2!=0)
    {
        str1[str1.length-1]=str1[str1.length-1]+"_";
    }
    else
    {
        str1=str1;
    }
    return (str1);
}
